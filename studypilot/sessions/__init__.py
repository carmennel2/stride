"""Sessions blueprint.

Logs stretches of study time against a task. Most of the UX lives on the
task detail page (the log-a-session form), so this blueprint mainly
exists to own the POST endpoints and the delete action.
"""
