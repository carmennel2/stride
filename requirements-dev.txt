# Development-only dependencies. Install with:
#   pip install -r requirements-dev.txt
# These are not required to run the application — only to lint and test it.
-r requirements.txt
pytest==7.4.4
pytest-flask==1.3.0
pytest-cov==4.1.0
ruff==0.2.0
mypy==1.8.0
