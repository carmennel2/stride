[pytest]
testpaths = tests
addopts = -ra --strict-markers -q
filterwarnings =
    # Quiet sklearn / SQLAlchemy deprecation noise that isn't actionable here.
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore:.*Query.get.*:sqlalchemy.exc.LegacyAPIWarning
